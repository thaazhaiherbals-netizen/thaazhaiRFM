# See .env.local.example and .env.production.example.
# The old shared .env file is no longer loaded.
# Default: APP_ENV=development -> .env.local
# Explicit live mode: APP_ENV=production -> .env.production / Render variables
